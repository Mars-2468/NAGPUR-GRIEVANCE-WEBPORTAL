<?php 

ini_set('display_errors',1);
function send_sms_new($message,$mobile,$templateId)
{
    
    
               /* $mobile;
				$message =str_replace ( ' ', '%20', $message);
				
				echo $url ="http://smsatm.net/v3/api.php?username=ASCDCL&apikey=c01f32640f54e44f7660&senderid=AMCGOV&templateid=".$templateId."&mobile=".$mobile."&message=".$message;
				exit;
				$post = curl_init();
                    curl_setopt($post,CURLOPT_SSL_VERIFYPEER, false);
                    curl_setopt($post, CURLOPT_URL, $url);
                    curl_setopt($post, CURLOPT_RETURNTRANSFER, 1);
                    $result=curl_exec($post); //result from mobile seva server
					if (curl_errno($post)) {
    echo $error_msg = curl_error($post);
}
exit;
                    
                    //output from server displayed
                    curl_close($post);*/
    
    

		
}
?> 